#[macro_use] extern crate rustonum;
//#![cfg_attr(features = "unstable")]

use rustonum::vector;

//#[cfg(features = "unstable")]
fn test_vec3_zero01()
{
    //let v: vector::Vector3<u32> = vector::Vector3::zero();
    //assert()
}
